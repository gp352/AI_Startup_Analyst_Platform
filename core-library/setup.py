from setuptools import setup, find_packages

setup(
    name="core-library",
    version="0.1.0",
    packages=find_packages(),
    description="A shared library for the AI Analyst platform's microservices."
)

